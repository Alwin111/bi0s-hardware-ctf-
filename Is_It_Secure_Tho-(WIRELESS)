# Is_It_Secure_Tho-(WIRELESS)
bi0s hardware -ctf

The task was to track down the link. so i opened the file. i opened it on wireshark
so clicked all the options in it then i found  an option called find packets and 
so the answer is as a string value so i clicked the string option and searched for wired
then i found the flag wired{s3cur3_4_ev4}
